{
  "name": "ptx-server",
  "version": "1.0.0",
  "description": "A proxy server for ptx",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "now",
    "remove-un-alias": "now rm ptx-server --safe --yes"
  },
  "keywords": [
    "now",
    "ptx",
    "proxy",
    "bus",
    "rail",
    "server"
  ],
  "author": "ike",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.7"
  },
  "dependencies": {
    "node-cache": "^4.2.0",
    "node-fetch": "^3.1.1"
  }
}
