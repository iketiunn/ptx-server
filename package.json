{
  "name": "ptx-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "now && now rm ptx-server --safe --yes"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.0.7"
  },
  "dependencies": {
    "node-cache": "^4.2.0",
    "node-fetch": "^2.6.0"
  }
}
